name: Preview Deploy

on:
  push:
    branches:
      - preview
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/preview-cd.yml
    with:
      last-deploy-commit: ${{ github.event.before }}
    secrets: inherit
