{% extends 'base.html.twig' %}

{% block content %}
<article class="markdown-body">
  {{ message_html|raw }}
  <ul>
    {% for item in lang_items %}
      <li><a href="{{ item.href }}">{{ item.code }}</a></li>
    {% endfor %}
  </ul>
</article>
{% endblock %}
