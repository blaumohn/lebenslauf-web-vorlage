{% extends 'cv_layout.html.twig' %}

{% block content %}
  {% import 'components/cv/view.html.twig' as cv_view %}
  {{ cv_view.view(
    cv,
    etiketten,
    true,
    cv.kopfdaten.name.voll|default(cv.kopfdaten.name),
    cv_footer
  ) }}
{% endblock %}
