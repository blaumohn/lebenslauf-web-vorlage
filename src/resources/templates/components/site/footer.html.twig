<footer>
  {% if privacy_note %}
    <div>{{ privacy_note }}</div>
  {% endif %}
  <div class="footer-meta">
    <span>{{ "now"|date("d.m.Y") }} {{ attribution }}</span>
    {% if built_with %}
      <span>{{ built_with|raw }}</span>
    {% endif %}
  </div>
</footer>
