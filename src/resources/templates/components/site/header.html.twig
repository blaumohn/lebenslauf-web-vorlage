<header>
  <a href="{{ path('/') }}" class="site-name">{{ site_name }}</a>
  <nav>
    {% for item in nav_items %}
      <a href="{{ path(item.href) }}">{{ item.label }}</a>
    {% endfor %}
  </nav>
</header>
