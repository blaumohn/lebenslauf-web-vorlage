<header>
  <a href="{{ path('/') }}" class="site-name">{{ site_name }}</a>
  <nav>
    <a href="{{ path('/') }}">Home</a>
    <a href="{{ path('/cv') }}">Lebenslauf</a>
    <a href="{{ path('/contact') }}">Kontakt</a>
  </nav>
</header>
