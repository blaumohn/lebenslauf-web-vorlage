<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{{ title|default('Lebenslauf') }}</title>
  <link rel="stylesheet" href="{{ path('/css/site.css') }}">
</head>
<body>
  {% import 'components/site/layout.html.twig' as layout %}
  <div class="page">
    {{ site_header|raw }}

    <main class="card">
      {% block content %}{% endblock %}
    </main>

    {{ layout.footer() }}
  </div>
</body>
</html>
