{% extends 'cv_layout.html.twig' %}

{% block content %}
  {% import 'components/cv/view.html.twig' as cv_view %}
  {{ cv_view.view(
    cv,
    etiketten,
    false,
    cv.kopfdaten.name.kurz|default(cv.kopfdaten.name),
    cv_footer
  ) }}
{% endblock %}
