app:
  APP_BASE_PATH: "/"
  TRUST_PROXY: 1
  APP_LOG_CHANNEL: file

content:
  CONTENT_PATH: .local
  CONTENT_LANGS: "de,en"

lebenslauf:
  LEBENSLAUF_PUBLIC_PROFILE: demo

captcha:
  CAPTCHA_TTL_SECONDS: 600
  CAPTCHA_MAX_GET: 10

contact:
  CONTACT_MAX_POST: 5

mail:
  MAIL_STDOUT: 0

rate_limit:
  RATE_LIMIT_WINDOW_SECONDS: 600

smtp:
  SMTP_PORT: 587
  SMTP_ENCRYPTION: tls
  SMTP_FROM_NAME: "Lebenslauf Seite"
